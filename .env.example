# Optional. Without this key the regional simulator uses OpenStreetMap.
# For production, enable Maps JavaScript API, restrict the key to your domains,
# and configure this value in Vercel rather than committing a real key.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
